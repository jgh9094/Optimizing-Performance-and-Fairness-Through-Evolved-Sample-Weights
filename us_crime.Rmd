# US Crime

Here we report the **hypervolume** achived by evaluating the performance of each solution wihtin the Pareto front on the test set of the `us_crime` dataset.

## Analysis setup

```{r usc-hv-data, message=FALSE}
# heart-disease data
data <- filter(testing, dataset == "us_crime")
```

## Hypervolume

```{r usc-hv-plt, echo=TRUE, message=FALSE, out.width="100%"}
volume_plotter(data,6)
```

### Summary stats

```{r usc-hv-stats, echo=TRUE, message=FALSE, out.width="100%"}
volume_summarize(data)
```

### Kruskal-Wallis test

Detected differences between weight strategies.

```{r usc-hv-kru, echo=TRUE, message=FALSE, out.width="100%"}
kruskal.test(hv ~ exp, data = data)
```

### Pairwise wlcoxon test

```{r usc-hv-wil, echo=TRUE, warning=FALSE}
pairwise.wilcox.test(x = data$hv, g = data$exp, p.adjust.method = "bonferroni",
                    paired = FALSE, conf.int = FALSE, alternative = 'l')
```