# Student Math

Here we report the **hypervolume** achived by evaluating the performance of each solution wihtin the Pareto front on the test set of the `student_math` dataset.

```{r sm-hv-data, message=FALSE}
# heart-disease data
data <- filter(testing, dataset == "student_math")
```

## Hypervolume

```{r sm-hv-plt, echo=TRUE, message=FALSE, out.width="100%"}
volume_plotter(data,2)
```

### Summary stats

```{r sm-hv-stats, echo=TRUE, message=FALSE, out.width="100%"}
volume_summarize(data)
```

### Kruskal-Wallis test

Detected differences between weight strategies.

```{r sm-hv-kru, echo=TRUE, message=FALSE, out.width="100%"}
kruskal.test(hv ~ exp, data = data)
```

### Pairwise wlcoxon test

```{r sm-hv-wil, echo=TRUE, warning=FALSE}
pairwise.wilcox.test(x = data$hv, g = data$exp, p.adjust.method = "bonferroni",
                    paired = FALSE, conf.int = FALSE, alternative = 'l')
```